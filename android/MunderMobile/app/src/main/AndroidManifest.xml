<?xml version="1.0" encoding="utf-8"?>
<!-- Espejo de ios/MunderMobile/Info.plist.
     El enlace habla http plano en LAN y Tailscale (el secreto va sellado
     ChaCha20-Poly1305 dentro, igual que en iOS): por eso cleartext permitido.
     Ver test/ios-ats.test.cjs para la justificación original. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Biometría / PIN del dueño: puerta de ver y de actuar, como Face ID. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:label="Munder"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MunderMobile">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
